{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Solution",
  "description": "Agent 2 fix proposal output. Reads only briefing.md and validated diagnosis.json; no repo inspection, no tools.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "run_id",
    "recommendation",
    "confidence",
    "no_fix_reason",
    "recommended_fix_id",
    "fixes",
    "weak_evidence",
    "weak_evidence_reason"
  ],
  "properties": {
    "run_id": {
      "type": "string",
      "minLength": 1
    },
    "recommendation": {
      "type": "string",
      "enum": ["FIX", "NO_FIX"]
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "no_fix_reason": {
      "type": ["string", "null"]
    },
    "recommended_fix_id": {
      "type": ["string", "null"]
    },
    "weak_evidence": {
      "type": "boolean",
      "description": "True if the orchestrator flagged the upstream diagnosis as weak (confidence below RCA_CONFIDENCE_WEAK_THRESHOLD or agent1a_quality weak/failed)."
    },
    "weak_evidence_reason": {
      "type": ["string", "null"],
      "description": "Explanation when weak_evidence=true. Null otherwise."
    },
    "fixes": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "description",
          "why_this_fixes_root_cause",
          "unified_diff",
          "affected_files",
          "risk",
          "expected_tests",
          "manual_review_notes"
        ],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "why_this_fixes_root_cause": { "type": "string", "minLength": 1 },
          "unified_diff": { "type": "string", "minLength": 1 },
          "affected_files": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 }
          },
          "risk": {
            "type": "string",
            "enum": ["low", "medium", "high"]
          },
          "expected_tests": {
            "type": "array",
            "items": { "type": "string" }
          },
          "manual_review_notes": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    }
  },
  "if": {
    "properties": { "recommendation": { "const": "FIX" } }
  },
  "then": {
    "properties": {
      "fixes": { "minItems": 1 },
      "recommended_fix_id": { "type": "string", "minLength": 1 }
    }
  }
}
